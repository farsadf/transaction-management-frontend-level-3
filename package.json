{
  "name": "transaction-management-frontend-level-3",
  "version": "0.0.1",
  "scripts": {
    "build": "cd ui && npm run build",
    "start": "cd ui && npm run serve",
    "test": "cypress run"
  },
  "devDependencies": {
    "cypress": "^8.7.0"
  }
}
